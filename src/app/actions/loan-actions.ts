'use server';

/**
 * @fileOverview Placeholder for loan-related server actions.
 */

export async function submitLoanWithAnalysis(formData: any) {
    // This action is currently not used after removing the AI appraisal system.
    return { success: true };
}
