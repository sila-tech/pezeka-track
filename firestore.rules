rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access for any authenticated user to any document.
    // This simplifies permissions to resolve the access errors.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
