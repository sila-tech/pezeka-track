
{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Stores user-specific information and role for staff, finance, and agents.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "role": { "type": "string", "enum": ["staff", "finance", "agent"] },
        "name": { "type": "string" },
        "referralCode": { "type": "string" },
        "referredBy": { "type": "string" }
      },
      "required": ["uid", "email", "role"]
    },
    "Customer": {
      "title": "Customer",
      "description": "Represents a customer of Pezeka Credit Ltd.",
      "type": "object",
      "properties": {
        "accountNumber": { "type": "string", "description": "Unique account number for the customer." },
        "name": { "type": "string" },
        "phone": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "idNumber": { "type": "string" },
        "referralCode": { "type": "string" },
        "referredBy": { "type": "string", "description": "UID of the user who referred this customer" },
        "referredByCode": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "phone"]
    },
    "Loan": {
      "title": "Loan",
      "description": "Represents a loan disbursed to a customer.",
      "type": "object",
      "properties": {
        "loanNumber": { "type": "string" },
        "customerId": { "type": "string" },
        "customerName": { "type": "string" },
        "customerPhone": { "type": "string" },
        "status": { "type": "string", "enum": ["active", "due", "overdue", "paid", "rollover", "application", "rejected"] },
        "principalAmount": { "type": "number" },
        "interestRate": { "type": "number" },
        "totalRepayableAmount": { "type": "number" },
        "totalPaid": { "type": "number", "default": 0 },
        "totalPenalties": { "type": "number", "default": 0 }
      }
    },
    "Referral": {
      "title": "Referral",
      "description": "Tracking record for a referral event.",
      "type": "object",
      "properties": {
        "referrerId": { "type": "string" },
        "referrerName": { "type": "string" },
        "refereeId": { "type": "string" },
        "refereeName": { "type": "string" },
        "status": { "type": "string", "enum": ["signed_up", "applied", "disbursed"] },
        "timestamp": { "type": "string", "format": "date-time" }
      }
    },
    "MailLog": {
      "title": "Mail Log",
      "description": "Records of emails sent by the system or staff.",
      "type": "object",
      "properties": {
        "recipient": { "type": "string" },
        "subject": { "type": "string" },
        "body": { "type": "string" },
        "type": { "type": "string" },
        "sentAt": { "type": "string", "format": "date-time" },
        "sender": { "type": "string" }
      }
    }
  },
  "auth": {
    "providers": ["password", "phone"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/UserProfile" },
      "description": "Stores user profiles."
    },
    "/customers/{customerId}": {
      "schema": { "$ref": "#/entities/Customer" },
      "description": "Stores all customer records."
    },
    "/loans/{loanId}": {
      "schema": { "$ref": "#/entities/Loan" },
      "description": "Stores all loan records."
    },
    "/referrals/{referralId}": {
      "schema": { "$ref": "#/entities/Referral" },
      "description": "Records of referral conversions."
    },
    "/financeEntries/{entryId}": {
      "description": "Stores all financial entries."
    },
    "/mail_logs/{logId}": {
      "schema": { "$ref": "#/entities/MailLog" },
      "description": "Logs of sent emails."
    },
    "/system_config/{configId}": {
      "description": "General application configuration and metadata."
    }
  }
}
