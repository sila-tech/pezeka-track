
{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Stores user-specific information and role for staff and finance.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "role": { "type": "string", "enum": ["staff", "finance"] },
        "status": { "type": "string", "enum": ["pending", "approved", "rejected"], "default": "approved" },
        "name": { "type": "string" }
      },
      "required": ["uid", "email", "role"]
    },
    "Customer": {
      "title": "Customer",
      "description": "Represents a customer of Pezeka Credit Ltd.",
      "type": "object",
      "properties": {
        "accountNumber": { "type": "string", "description": "Unique account number for the customer." },
        "name": { "type": "string" },
        "phone": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "idNumber": { "type": "string" },
        "referralCode": { "type": "string", "description": "Unique code for this customer to refer others." },
        "referredBy": { "type": "string", "description": "The referral code of the person who invited this customer." },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "phone", "idNumber"]
    },
    "Loan": {
      "title": "Loan",
      "description": "Represents a loan disbursed to a customer.",
      "type": "object",
      "properties": {
        "loanNumber": { "type": "string" },
        "customerId": { "type": "string" },
        "customerName": { "type": "string" },
        "customerPhone": { "type": "string" },
        "status": { "type": "string", "enum": ["active", "due", "overdue", "paid", "rollover", "application", "rejected"] },
        "principalAmount": { "type": "number" },
        "interestRate": { "type": "number" },
        "totalRepayableAmount": { "type": "number" },
        "totalPaid": { "type": "number", "default": 0 },
        "totalPenalties": { "type": "number", "default": 0 }
      }
    },
    "KYCDocument": {
      "title": "KYC Document",
      "description": "Metadata and image data for customer KYC documents.",
      "type": "object",
      "properties": {
        "customerId": { "type": "string" },
        "customerName": { "type": "string" },
        "type": { "type": "string", "enum": ["owner_id", "guarantor_id", "loan_form", "security_attachment", "guarantor_undertaking"] },
        "fileName": { "type": "string" },
        "fileUrl": { "type": "string", "description": "Data URI or URL of the captured image." },
        "uploadedBy": { "type": "string" },
        "uploadedAt": { "type": "string", "format": "date-time" }
      }
    },
    "MailLog": {
      "title": "Mail Log",
      "description": "Records of emails sent by the system or staff.",
      "type": "object",
      "properties": {
        "recipient": { "type": "string" },
        "subject": { "type": "string" },
        "body": { "type": "string" },
        "type": { "type": "string" },
        "sentAt": { "type": "string", "format": "date-time" },
        "sender": { "type": "string" }
      }
    }
  },
  "auth": {
    "providers": ["password", "phone"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/UserProfile" },
      "description": "Stores user profiles."
    },
    "/customers/{customerId}": {
      "schema": { "$ref": "#/entities/Customer" },
      "description": "Stores all customer records."
    },
    "/kyc_documents/{docId}": {
      "schema": { "$ref": "#/entities/KYCDocument" },
      "description": "Master repository for all customer KYC documents."
    },
    "/loans/{loanId}": {
      "schema": { "$ref": "#/entities/Loan" },
      "description": "Stores all loan records."
    },
    "/financeEntries/{entryId}": {
      "description": "Stores all financial entries."
    },
    "/mail_logs/{logId}": {
      "schema": { "$ref": "#/entities/MailLog" },
      "description": "Logs of sent emails."
    },
    "/system_config/{configId}": {
      "description": "General application configuration and metadata."
    }
  }
}
